package Snova.Std.IO

interface Reader {
    method read(maxLen: int): string
}

interface Writer {
    method write(data: string): int
}

interface Closer {
    method close(): unit
}

interface Stream : Reader, Writer, Closer {
}

interface Handle : Closer {
}
